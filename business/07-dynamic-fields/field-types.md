# Purpose
Định nghĩa các kiểu dữ liệu hỗ trợ cho Dynamic Field.

# Supported Types
- Core: `TEXT`, `LONG_TEXT`, `NUMBER`, `DECIMAL`, `BOOLEAN`, `DATE`, `DATETIME`, `SELECT`, `MULTI_SELECT`.
- (Tương lai/Out of scope trừ khi kiến trúc có sẵn: JSON, Formula).
